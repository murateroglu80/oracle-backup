# ============================================================
# secrets/secrets_local.yaml — CREDENTIALS_CONFIG.provider: "local" iken (spec §2.3)
# Vault/CyberArk yokken düz-metin fallback. 'secrets_local.yaml' olarak kopyalayın.
# GÜVENLİK: credential'lar diskte PLAINTEXT. .gitignore'dadır; `chmod 600` ZORUNLU.
# Anahtar = instance_id. Eksik instance eşleşmesi FAIL-FAST'tir (Vault ile simetrik).
# ============================================================

LOCAL_INSTANCES:
  db-server1.example.local_orcl1:
    db:
      username: "rman_backup"
      password: "DEĞİŞTİR_BENİ"
      hostname: "db-server1.example.local"
      db: "ORCL1"
    smtp_password: "DEĞİŞTİR_BENİ"
